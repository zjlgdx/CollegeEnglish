<Page
    x:Class="CollegeEnglish.VocabularyPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CollegeEnglish"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding DefaultViewModel, RelativeSource={RelativeSource Self}}"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid x:Name="LayoutRoot">

        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title Panel -->
        <StackPanel Grid.Row="0" Margin="19,0,0,0">
            <TextBlock Text="College English" Style="{ThemeResource TitleTextBlockStyle}" Margin="0,12,0,0"/>
            <TextBlock Text="词汇" Margin="0,-6.5,0,26.5" Style="{ThemeResource HeaderTextBlockStyle}" CharacterSpacing="{ThemeResource PivotHeaderItemCharacterSpacing}"/>
        </StackPanel>

        <!--TODO: Content should be placed within the following grid-->
        <Grid Grid.Row="1" x:Name="ContentRoot" Margin="19,9.5,19,0">
            <Pivot x:Uid="Pivot" x:Name="pivot" CommonNavigationTransitionInfo.IsStaggerElement="True">
            <!--Pivot item one-->
            <PivotItem
                x:Uid="PivotItem1"
                Margin="19,14.5,0,0"
                Header="词汇A"
                DataContext="{Binding NewWordsA}"
                CommonNavigationTransitionInfo.IsStaggerElement="True">
                <!--Double line list with text wrapping-->

                    <FlipView x:Name="NewWordsA" 
                              Width="480" Height="270" 
                              BorderBrush="Black" 
                              BorderThickness="1"
                              ItemsSource="{Binding}">
                        <FlipView.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Border Background="#A5000000" Height="80">
                                        <TextBlock Text="{Binding Word}" FontFamily="Segoe UI" FontSize="26.667" Foreground="#CCFFFFFF" Padding="15,20"/>
                                    </Border>
                                    <Border BorderThickness="0,1,0,0" BorderBrush="LightSeaGreen" Margin="5,15">
                                        
                                    </Border>
                                    
                                    <Border>
                                        <StackPanel>
                                            <TextBlock Text="{Binding WordPhrase}" FontFamily="Segoe UI" FontSize="26.667" Foreground="#CCFFFFFF" Padding="15,20"/>
                                            <TextBlock Text="{Binding Sentence}" FontFamily="Segoe UI" FontSize="26.667" Foreground="#CCFFFFFF" Padding="15,20"/>
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </FlipView.ItemTemplate>
                    </FlipView>
                    
            </PivotItem>

            

            <!--Pivot item three-->
            <PivotItem
                x:Uid="PivotItem3"
                Margin="19,14.5,0,0"
                Header="词汇B"
                DataContext="{Binding NewWordsB}"
                >
                <!--Double line list no text wrapping-->
                <FlipView x:Name="NewWordsB" 
                              Width="480" Height="270" 
                              BorderBrush="Black" 
                              BorderThickness="1"
                              ItemsSource="{Binding}">
                        <FlipView.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Border Background="#A5000000" Height="80">
                                        <TextBlock Text="{Binding Word}" FontFamily="Segoe UI" FontSize="26.667" Foreground="#CCFFFFFF" Padding="15,20"/>
                                    </Border>
                                    <Border BorderThickness="0,1,0,0" BorderBrush="LightSeaGreen" Margin="5,15">

                                    </Border>

                                    <Border>
                                        <StackPanel>
                                            <TextBlock Text="{Binding WordPhrase}" FontFamily="Segoe UI" FontSize="26.667" Foreground="#CCFFFFFF" Padding="15,20"/>
                                            <TextBlock Text="{Binding Sentence}" FontFamily="Segoe UI" FontSize="26.667" Foreground="#CCFFFFFF" Padding="15,20"/>
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </FlipView.ItemTemplate>
                    </FlipView>
                </PivotItem>

            
            </Pivot>
        </Grid>
    </Grid>
</Page>
